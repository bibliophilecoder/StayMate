import { router } from "expo-router";import { useState } from "react";import { Alert,Text,View } from "react-native";import { SafeAreaView } from "react-native-safe-area-context";import { Button,Field } from "@/components/ui";import { resetPassword } from "@/services/firebase";
export default function Forgot(){const[email,setEmail]=useState("");const send=async()=>{try{await resetPassword(email.trim());Alert.alert("Check your inbox","If that email is registered, Firebase has sent a reset link.",[{text:"OK",onPress:()=>router.back()}])}catch(e){Alert.alert("Couldn’t send link",e instanceof Error?e.message:"Try again")}};return <SafeAreaView className="flex-1 bg-mist"><View className="flex-1 justify-center px-6"><Text className="text-4xl font-black text-ink">Reset password</Text><Text className="mb-8 mt-2 text-slate-500">We’ll send a secure reset link to your inbox.</Text><Field label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" placeholder="you@example.com"/><Button title="Send reset link" onPress={()=>void send()}/></View></SafeAreaView>}
