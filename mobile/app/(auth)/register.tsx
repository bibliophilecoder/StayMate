import { router } from "expo-router";import { useState } from "react";import { Text,View } from "react-native";import { SafeAreaView } from "react-native-safe-area-context";import { Button,Field } from "@/components/ui";import { emailRegister } from "@/services/firebase";import { useApp } from "@/context/AppContext";
export default function Register(){const{setAuthenticated,refresh}=useApp();const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const submit=async()=>{try{setError("");if(name.trim().length<2||password.length<8)throw new Error("Enter your name and an 8-character password");await emailRegister(name.trim(),email.trim(),password);setAuthenticated(true);await refresh();router.replace("/(tabs)/profile")}catch(e){setError(e instanceof Error?e.message:"Registration failed")}};return <SafeAreaView className="flex-1 bg-mist"><View className="flex-1 justify-center px-6"><Text className="text-4xl font-black text-ink">Make yourself at home</Text><Text className="mb-8 mt-2 text-slate-500">Create your account, then tell us what comfortable living means to you.</Text><Field label="Full name" value={name} onChangeText={setName} placeholder="Your name"/><Field label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" placeholder="you@example.com"/><Field label="Password" value={password} onChangeText={setPassword} secureTextEntry placeholder="At least 8 characters"/>{error&&<Text className="mb-3 text-sm text-red-500">{error}</Text>}<Button title="Create account" onPress={()=>void submit()}/><Text onPress={()=>router.replace("/(auth)/login")} className="mt-6 text-center font-semibold text-primary">Already a member? Log in</Text></View></SafeAreaView>}

