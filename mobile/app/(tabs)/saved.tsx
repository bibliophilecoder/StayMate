import { Text,View } from "react-native";import { SafeAreaView } from "react-native-safe-area-context";import { useApp } from "@/context/AppContext";import { ProfileCard } from "@/components/ProfileCard";
export default function Saved(){const{profiles,bookmarks}=useApp();const saved=profiles.filter(p=>bookmarks.includes(p.id));return <SafeAreaView className="flex-1 bg-mist"><View className="px-5 pt-4"><Text className="text-3xl font-black text-ink">Saved profiles</Text>{saved.length?<View className="mt-5"><ProfileCard profile={saved[0]}/></View>:<View className="mt-24 items-center"><Text className="text-5xl">🔖</Text><Text className="mt-4 text-xl font-extrabold text-ink">Save someone for later</Text><Text className="mt-2 text-center text-slate-500">Tap the bookmark while discovering profiles.</Text></View>}</View></SafeAreaView>}

