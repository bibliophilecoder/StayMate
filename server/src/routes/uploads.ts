import { Router } from "express";import { v2 as cloudinary } from "cloudinary";import { requireAuth } from "../middleware/auth.js";import { env } from "../config/env.js";const router=Router();router.get("/signature",requireAuth,(_req,res)=>{if(!env.CLOUDINARY_API_KEY||!env.CLOUDINARY_API_SECRET||!env.CLOUDINARY_CLOUD_NAME)return res.status(503).json({error:"Cloudinary is not configured"});cloudinary.config({cloud_name:env.CLOUDINARY_CLOUD_NAME,api_key:env.CLOUDINARY_API_KEY,api_secret:env.CLOUDINARY_API_SECRET});const timestamp=Math.round(Date.now()/1000);const folder="staymate/profiles";const signature=cloudinary.utils.api_sign_request({timestamp,folder},env.CLOUDINARY_API_SECRET);res.json({timestamp,folder,signature,cloudName:env.CLOUDINARY_CLOUD_NAME,apiKey:env.CLOUDINARY_API_KEY})});export default router;
