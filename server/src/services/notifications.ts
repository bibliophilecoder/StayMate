export async function sendPush(tokens:string[],title:string,body:string,data:Record<string,string>={}){const valid=[...new Set(tokens)].filter(x=>x.startsWith("ExponentPushToken[")||x.startsWith("ExpoPushToken["));if(!valid.length)return;const response=await fetch("https://exp.host/--/api/v2/push/send",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(valid.map(to=>({to,title,body,data,sound:"default",channelId:"messages"})))});if(!response.ok)throw new Error(`Expo push service returned ${response.status}`)}
